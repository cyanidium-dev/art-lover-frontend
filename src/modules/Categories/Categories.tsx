import categoryItems from "@/modules/Categories/categoryItems";

const Categories = ()=> {
    // const elements = categoryItems.map(categoryItem => ({}))
    return <div></div>
}

export default  Categories;
